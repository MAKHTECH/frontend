# Быстрый старт gRPC интеграции

## Что было добавлено

✅ **gRPC-Web клиент** для работы с вашим сервером  
✅ **Proto файл** с определением сервисов аутентификации  
✅ **AuthService** для регистрации и входа  
✅ **Интеграция с AuthModal** - работает "из коробки"  

## Что нужно сделать

### 1. Настроить адрес сервера

Откройте `src/config/grpc.config.js` и измените адрес (если нужно):

```javascript
export const GRPC_CONFIG = {
  serverUrl: 'http://localhost:8080', // Envoy proxy
  grpcServerUrl: 'http://localhost:44044', // ← Ваш gRPC сервер
};
```

### 2. Запустить Envoy Proxy

gRPC-Web требует прокси между браузером и gRPC сервером.

**Docker Compose (рекомендуется):**
```bash
docker-compose up -d
```

Проверить статус:
```bash
docker-compose logs -f envoy
```

Остановить:
```bash
docker-compose down
```

**envoy.yaml уже настроен** на порт **44044** (ваш gRPC сервер)

### 3. Проверить работу

Запустите frontend:
```bash
npm run dev
```

Откройте браузер, нажмите на кнопку регистрации и проверьте Console (F12):
- Должен отправиться запрос к вашему серверу
- В консоли будут логи запросов/ответов

## Структура запроса

### Register Request (от клиента)
```json
{
  "email": "user@example.com",
  "username": "myusername",
  "password": "password123",
  "app_id": 1
}
```

### Register Response (от сервера)
```json
{
  "tokens": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "refresh_token_here"
  }
}
```

### Login Request (от клиента)
```json
{
  "username": "myusername",
  "password": "password123",
  "app_id": 1
}
```

### Login Response (от сервера)
```json
{
  "tokens": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "refresh_token_here"
  }
}
```

## Отладка

### Проверить, работает ли Envoy:
```bash
curl http://localhost:8080
```

### Проверить запросы в консоли браузера:
1. F12 → Network
2. Попробуйте зарегистрироваться
3. Найдите запрос к `/auth.AuthService/Register`

### Типичные ошибки:

❌ **CORS Error** → Проверьте Envoy конфигурацию  
❌ **Connection refused** → Проверьте, запущен ли Envoy и gRPC сервер  
❌ **404 Not Found** → Проверьте URL в `grpc.config.js`  

## Файлы для изменения

📄 `src/config/grpc.config.js` - адрес сервера (уже настроен на порт 44044)  
📄 `envoy.yaml` - настройки прокси (уже настроен на порт 44044)  
📄 `proto/sso/*.proto` - определения gRPC сервисов  

## Доступные методы

### Auth Service (`authService.js`)
- `register(email, username, password, appId)` - регистрация
- `login(username, password, appId)` - вход
- `logout()` - выход
- `refreshToken()` - обновление токенов
- `getDevices(userId)` - список устройств пользователя
- `getAccessToken()` - получить access token
- `getRefreshToken()` - получить refresh token

### User Service (`userService.js`)
- `assignRole(userId, appId, role)` - назначить роль пользователю  

## Дополнительно

📖 Полная документация: `GRPC_SETUP.md`  
📝 Примеры кода: `src/examples/authExamples.js`  

## Готово! 🚀

Теперь при нажатии кнопки "Зарегистрироваться" в модальном окне будет отправляться gRPC запрос на ваш сервер.
